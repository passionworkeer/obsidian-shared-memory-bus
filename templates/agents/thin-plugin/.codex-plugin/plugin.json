{
  "name": "shared-memory-thin-plugin",
  "version": "0.0.1",
  "description": "Thin host adapter for shared HTTP MCP + portable skill",
  "entrypoint": "index.js",
  "interface": {
    "displayName": "Shared Memory Bus",
    "shortDescription": "Connects the host AI tool to the shared HTTP MCP memory bus",
    "defaultPrompt": "Connect this host to the shared HTTP MCP endpoints, load the portable shared-memory skill/rule file, and keep host-native logic as thin glue only."
  }
}
