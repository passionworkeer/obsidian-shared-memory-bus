# Local AI Memory Bus — Environment Configuration
# Copy this file to .env and adjust for your setup.

# =============================================================================
# STORE ROOT (required)
# =============================================================================
# The root directory for all memory data (.ai-memory store).
# Defaults to ~/.ai-memory on all platforms.
AI_MEMORY_ROOT=<your-store-root-path>
# Aliases (also checked, in order):
# AI_MEMORY_STORE=
# AI_MEMORY_STORE_ROOT=

# =============================================================================
# PYTHON RUNTIME (optional)
# =============================================================================
# Path to Python executable for retrieval engine.
# Auto-detected if not set.
# AI_MEMORY_PYTHON=D:\python\python.exe

# =============================================================================
# EMBEDDINGS (optional — falls back to local hash embeddings)
# =============================================================================
# Embedding backend: "openai" | "hash" (default: "hash" = local LSH hash, no API needed)
# AI_MEMORY_EMBED_BACKEND=hash

# For OpenAI-compatible embedding providers:
# AI_MEMORY_EMBED_BASE_URL=https://api.openai.com/v1
# AI_MEMORY_EMBED_API_KEY=sk-your-key-here
# AI_MEMORY_EMBED_MODEL=text-embedding-3-small
# AI_MEMORY_EMBED_TIMEOUT_SECONDS=180
# AI_MEMORY_EMBED_REQUEST_DELAY_MS=1000

# =============================================================================
# MCP SERVER PORTS (optional)
# =============================================================================
# These are configured in start.js — edit that file to change ports.
# Default ports:
#   Fetch MCP:  9332
#   Time MCP:    9333
#   Memory MCP:  9338
#   Analysis:    9331
#   Search:      9334
#   Health:      9335

# =============================================================================
# RUNTIME CONFIG (optional)
# =============================================================================
# Path to runtime config JSON. Overrides runtime.json in the store root.
# AI_MEMORY_RUNTIME_CONFIG_PATH=

# Allow embedding runtime env vars to override runtime.json (e.g. CI env).
# AI_MEMORY_ALLOW_EMBED_RUNTIME_ENV_OVERRIDES=false

# Base port for shared MCP servers (default 9331; per-server port = base + offset).
# AI_MEMORY_BASE_PORT=9331

# =============================================================================
# CLAUDE-MEM BRIDGE (optional)
# =============================================================================
# Base URL for the claude-mem compatibility API used by query_claude_mem /
# insert_claude_mem tools. Defaults to http://127.0.0.1:37778.
# CLAUDE_MEM_BASE=http://127.0.0.1:37778

# =============================================================================
# REDACTION (optional)
# =============================================================================
# PII redaction for outbound tool output and search results.
# AI_MEMORY_REDACTION_ENABLED=true
# AI_MEMORY_REDACTION_MODE=tools
# AI_MEMORY_REDACTION_PLACEHOLDER=[REDACTED]

# =============================================================================
# EMBEDDINGS BATCH (optional)
# =============================================================================
# Override default embedding batch size. Default 24.
# AI_MEMORY_EMBED_BATCH_SIZE=24

# =============================================================================
# OBSIDIAN BACKWARD COMPATIBILITY (deprecated)
# =============================================================================
# Kept only for legacy integrations that still reference Obsidian vault.
# Not used by the core memory bus.
# AI_MEMORY_OBSIDIAN_VAULT=
# OBSIDIAN_VAULT_ROOT=